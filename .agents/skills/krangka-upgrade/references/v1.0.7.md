# Changes from v1.0.6 to v1.0.7

## Dependencies

No `go.mod` / `go.sum` changes versus v1.0.6.

## Added

Nothing.

## Removed

Nothing.

## Modified

### `krangka-upgrade` agent skill (workflow for AI agents)

Files (keep identical byte-for-byte across trees):

- `.agents/skills/krangka-upgrade/SKILL.md`
- `.cursor/skills/krangka-upgrade/SKILL.md`
- `.claude/skills/krangka-upgrade/SKILL.md`

**Behavior change:** Step 4 of **Upgrade Workflow** now requires cloning the upstream boilerplate at the **exact** tag being applied (`vX.Y.Z`) before writing any application code from a reference note. Reference markdown is high-level; implementations (signatures, bodies, imports, ordering) must be taken from `https://github.com/redhajuanda/krangka` at that tag, not invented from the summary.

**Concrete edits to reproduce:**

1. After “Read the reference file”, insert a numbered step **CRITICAL — Clone the exact upstream version before applying any code change** that instructs:
   - Clone shallow or checkout: `git clone --depth 1 --branch vX.Y.Z https://github.com/redhajuanda/krangka /tmp/krangka-vX.Y.Z` (or reuse a clone and `git checkout vX.Y.Z`).
   - For each **Added** / **Modified** item, copy **exact** code from the clone at that path.
   - For **Modified** items, use `git diff vPREV vX.Y.Z -- <path>` on the clone to see precise edits.
   - State that the upstream tag is the source of truth; the reference file alone is not sufficient for code.
2. Renumber the following steps: “Apply” becomes step 3 (with bullets stating Added/Removed/Modified/Dependencies use upstream as authoritative; **Dependencies** bullet: align `go.mod` with upstream at that tag and run `go mod tidy`), “Follow migration notes” step 4, “Update `.krangka/.VERSION`” step 5.
3. Under **Rules**, add: **Never invent or paraphrase code from a reference file.** Always clone `github.com/redhajuanda/krangka` at the target tag and copy the exact implementation from there.

**Authoritative source for the full file:** check out tag `v1.0.7` on `github.com/redhajuanda/krangka` and copy the three `SKILL.md` paths above from that revision (or apply this section and verify with `diff` against the tag after release).

## Migration notes

- Sync the three `krangka-upgrade/SKILL.md` paths (or run `krangka skill upgrade` from a CLI that ships this template) so all agent layouts match.
- No Go commands required for this release unless you bundle other local edits.
- Set `.krangka/.VERSION` to `v1.0.7` after applying this release to your project.
