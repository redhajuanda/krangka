# Changes from v1.0.5 to v1.0.6

## Dependencies

- `github.com/redhajuanda/qwery` `v1.0.3` → `v1.0.4`

No other direct dependency changes versus v1.0.5. After merging `go.mod` / `go.sum`, run `go mod tidy` and `go build ./...`. Review qwery release notes for any behavioral or breaking changes in SQL binding or pagination if you relied on internals.

## Added

Nothing.

## Removed

Nothing.

## Modified

Nothing.

## Migration notes

- Run `go mod tidy` after updating `go.mod` / `go.sum`.
- Confirm with `go build ./...`.
- Set `.krangka/.VERSION` to `v1.0.6` after applying this release to your project.
